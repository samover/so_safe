describe('ButtonController', function(){
  var ctrl;
  beforeEach(module('SoSafe'));

  beforeEach(inject(function($controller){
    ctrl = $controller('LoginController');
  }));

  describe('login in with username', function(){
    
  });
});
